# ApiRest-Laravel-6

Base for API REST Laravel 6 with Passport

## Install

    $ composer install
    $ cp .env.example .env
    $ php artisan key:generate
    $ php artisan migrate
    $ php artisan passport:install
